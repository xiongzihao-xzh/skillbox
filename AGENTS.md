## Agent skills

### Issue tracker

任务和规格通过 GitHub Issues 跟踪。读取或操作任务前，先读取
`docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认 triage 标签。分类任务或变更标签前，先读取
`docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 布局。探索代码库或使用领域术语前，先读取
`docs/agents/domain.md`。
