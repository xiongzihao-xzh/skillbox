# Skillbox

本仓库管理可复用的开发指导 Skill。本术语表记录各 Skill 的特有概念，供需求讨论、规则编写和验收使用。

## Language

### cola-java 的规则与审查概念

**cola-java**：
面向明确采用 Alibaba COLA 标准多模块方案的项目的开发指导 Skill，整合经核验的 COLA 架构规则与本任务增加的开发约束。
_Avoid_: COLA 业务系统、COLA 官方 Skill

**研究基线**：
形成 COLA 规则结论时固定的上游版本与来源集合，界定结论的适用范围和追溯依据。
_Avoid_: 未核验的“最新版”

**COLA 规则**：
有研究基线中的官方说明、模板或实现作为依据的架构和开发规则；某个示例的具体写法不自动构成普遍约束。
_Avoid_: 将项目偏好称为“COLA 官方强制规定”

**项目约定**：
本任务在 COLA 规则之外增加的编码、API 和类规模约束，是所有使用 cola-java 的项目默认适用的强制规则。
_Avoid_: COLA 官方规则、仅供参考的建议

**技术适配**：
为满足 cola-java 的目标技术环境而增加或调整的兼容性约束，与上游架构规则分开归类。

**建议实践**：
具有适用条件、可按实际需要选用的做法，不属于强制规则。

**业务逻辑类**：
实际承载业务规则、流程编排或用例执行逻辑的手写 Java 类，其归类取决于职责而非名称或注解。
_Avoid_: 仅把 Service 或 Executor 视为业务逻辑类

**声明型或数据型类**：
职责单一、主要承载字段、注解、配置声明或简单访问方法的类；包含复杂业务行为的类不属于此类。
_Avoid_: 将所有 Entity、DTO 或 Configuration 自动归入此类

**存量超限问题**：
本次变更前已存在、按业务逻辑类规模约束判定的超限问题；暂时保留并不等于合规或获得例外。

### 销售订单示例

**销售订单示例**：
cola-java 用于讲解与验证开发规则的教学业务场景，涵盖订单创建、查询和取消；其中的业务约束不代表真实订单系统的通用要求。
_Avoid_: 生产级订单平台

**订单取消**：
销售订单示例中终止订单继续履行的业务行为，不包含支付退款、库存释放或发货处理。

**取消记录**：
销售订单示例中每个订单至多一份的取消凭据，保存首次取消的原因和时间，后续重试引用同一份记录。
_Avoid_: 每次重试新增的取消事件、可任意改写的订单状态
