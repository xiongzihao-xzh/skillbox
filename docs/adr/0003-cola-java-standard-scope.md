# 首版聚焦标准多模块方案

cola-java 首版仅支持经本地仓库核验的官方 Web 后端标准多模块方案，教学和验收使用同一架构基线。用户已明确排除 light 方案，因此首版不提供其开发指导或完整示例；识别到已有 light 项目时，应说明当前覆盖范围，不擅自将其迁移为标准多模块结构。

完整销售订单示例采用内存实现，覆盖领域、应用和 HTTP 测试，支付、库存和发货留在示例之外。数据库事务边界写入开发指导，但必须明确真实数据库事务尚未实测，不能由内存测试推导数据库事务正确。
